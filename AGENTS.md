## Agent skills

### Issue tracker

GitHub Issues (uses the `gh` CLI). External PRs are not a triage surface — issues only. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles; label strings equal role names (defaults). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/architecture/adrs/` at the repo root. See `docs/agents/domain.md`.

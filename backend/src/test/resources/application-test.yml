spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tracetick
    username: tracetick
    password: tracetick

tracetick:
  bootstrap:
    enabled: false

logging:
  level:
    com.tracetick: DEBUG
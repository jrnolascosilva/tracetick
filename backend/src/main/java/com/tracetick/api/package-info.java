/**
 * HTTP boundary: REST controllers, request and response DTOs, bean-validation, and error mapping.
 *
 * <p>Depends on {@code domain} and {@code persistence} (for repositories). In v1 it exposes the
 * versioned API surface under {@code /api/v1/...}.
 */
package com.tracetick.api;